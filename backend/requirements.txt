# ── Clinic Dashboard — backend base requirements ────────────────────────────
# Light enough to install now. The heavy *local* ML stack lives in
# requirements-ml.txt and is only needed if you choose the "local models"
# AI approach instead of the Claude API. Pins are loose for hackathon speed.

# --- Web / API framework ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # file uploads (labs, scans)
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27

# --- AI model layer (sponsor: OpenAI vision/text for extraction only) ---
openai>=1.40                  # GPT vision/text — extraction stage (not severity)
anthropic>=0.34               # alternative provider; harmless to keep if unused

# --- Image processing (scans, photos of reports) ---
Pillow>=10.3                  # general image I/O
numpy>=1.26
opencv-python-headless>=4.9   # preprocessing: deskew, threshold, crop
pydicom>=2.4                  # DICOM medical imaging files
PyMuPDF>=1.24                 # render/extract PDF scans (import: fitz)
pdf2image>=1.17               # PDF -> images (needs poppler system pkg)
pytesseract>=0.3.10           # OCR for scanned docs (needs tesseract binary)

# --- Text processing (lab reports, case descriptions) ---
pypdf>=4.2                    # extract text from PDF reports
python-docx>=1.1              # .docx clinical notes
pandas>=2.2                   # tabular lab results
spacy>=3.7                    # tokenization / base NLP pipeline

# --- Medical terms understanding (lightweight layer) ---
#   For real entity linking to UMLS/SNOMED, enable scispaCy in
#   requirements-ml.txt OR rely on the Claude API. These help meanwhile:
rapidfuzz>=3.9                # fuzzy match against term dictionaries
# medspacy>=1.0               # rule-based clinical NLP (uncomment if wanted)
